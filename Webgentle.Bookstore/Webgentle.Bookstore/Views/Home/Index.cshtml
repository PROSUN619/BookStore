@*Coming from layout page will be placed at bottom after layout script tag*@
@section scripts{
    <script>
        $(document).ready(() => {
            //alert('document is ready');
        });
    </script>
}

@{
  dynamic data = ViewBag.Data;
  var defaultBook = new BookModel()
  {
      Id= 0,
      CoverImageURL="/books/cover/default.jpg",
      Title = "Book Title",
      Description = "Book Description",
      Author = "No Author"
  };
}

@{
  var viewData = ViewData["bookObject"] as BookModel;
  ViewData["Title"] = "Home";
}

<div class="px-4 my-5 text-center">
    <h1 class="display-5 fw-bold">Book Store Application</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4">
        <p>@ViewData["MyProperty"] @*this from viewdata attribute*@</p>
            @*@ViewBag.Book.Title <br/>*@
            Quickly design and customize responsive mobile-first sites with Bootstrap,
            the world’s most popular front-end open source toolkit, featuring Sass variables and mixins,
            responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.
        </p>
        <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <button type="button" class="btn btn-primary btn-lg px-4 gap-3">Primary button</button>
        </div>
    </div>
</div>

<div class="py-5 bg-light">
    <div class="container">
        @*<h3 class="h3">Top Books</h3>*@
        <!--Calling view component-->
        @(await Component.InvokeAsync("TopBook",new { count = 3 }))
        @*<vc:top-book count="1"></vc:top-book>*@
        <!--End Calling view component-->
        @*<div class="row">
            <div class="col-md-4 justify-content-sm-center">
                <partial name="_BookThumbnail" model="defaultBook" />
            </div>
            <div class="col-md-4 justify-content-sm-center">
                <partial name="_BookThumbnail" model="defaultBook" />
            </div>
            <div class="col-md-4 justify-content-sm-center">
                <partial name="_BookThumbnail" model="defaultBook" />
            </div>
        </div>*@
    </div>
</div>

